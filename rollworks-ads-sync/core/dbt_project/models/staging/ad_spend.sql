{{ config(materialized='table') }}
SELECT
    SNAPSHOT_DATE,
    CAMPAIGN_ID,
    CAMPAIGN_NAME,
    CAMPAIGN_CREATED_DATE,
    CAMPAIGN_START_DATE,
    CAMPAIGN_END_DATE,
    CAMPAIGN_STATUS,
    CAMPAIGN_UPDATED_DATE,
    CAMPAIGN_CHANNEL,
    CAMPAIGN_CURRENCY,
    AD_ID,
    AD_NAME,
    AD_DESTINATION_URL,
    AD_CREATED_DATE,
    AD_UPDATED_DATE,
    AD_STATUS,
    CAMPAIGN_TYPE,
    IMPRESSIONS,
    CLICKS,
    COST,
    CONVERSIONS,
    LAST_UPDATED_DATETIME
FROM {{ env_var('SNOWFLAKE_DATABASE') }}.{{ env_var('SNOWFLAKE_SCHEMA') }}.ad_spend
