SELECT
    TO_TIMESTAMP(EXTRACTION_DATE, 'DD/MM/YYYY HH24:MI:SS') AS EXTRACTION_DATE,
    TECHNICAL_COMPANY_CODE,
    UNIQUE_ACTION_CODE,
    COLLECTOR_IN_CHARGE,
    ACTION_CODE,
    ACTION_LABEL,
    ORIGIN,
    CREATION_DATE,
    EXPECTED_DATE,
    EFFECTIVE_DATE,
    VIEW_DATE,
    ACTION_TYPE,
    MEDIA_TYPE,
    DETAILS,
    AUTO,
    STATE,
    ACTION_BY,
    CANCELLATION_REASON,
    ACTION_AMOUNT,
    LAST_UPDATED_DATETIME
FROM {{ env_var('SNOWFLAKE_DATABASE') }}.{{ env_var('SNOWFLAKE_SCHEMA') }}.ACTIONS